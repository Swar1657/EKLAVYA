[server]
# Listen on all network interfaces instead of just localhost
headless = true
address = "0.0.0.0"
port = 8501

[logger]
level = "info"

[client]
showErrorDetails = true
toolbarMode = "viewer"
